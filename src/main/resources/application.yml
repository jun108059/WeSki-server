spring:
  application:
    name: weski
  datasource:
    url: jdbc:mysql://localhost:3306/ski_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: master
    password: master
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true